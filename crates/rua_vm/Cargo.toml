[package]
name = "rua_vm"
version = "0.1.0"
edition = "2024"

[features]
default = []
builtin_sha256_verify = ["dep:sha2"]

[dependencies]
rua = { path = "../.." }
sha2 = { version = "0.10", optional = true }
